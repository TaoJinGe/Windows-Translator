AI 文件系统索引协议（完整最终版）

本文件是整个项目唯一规则文件（不可拆分、不可省略）。

AI每次执行任务前必须完整读取本文件。

==================================================
一、系统本质定义

本系统是一个：

👉 “多层级、可拆分、可扩展的文件系统索引协议”

用于让AI在任何项目中：

精确定位文件
避免全局扫描
控制上下文长度
持续同步结构变化
==================================================
二、系统结构（核心概念）

整个系统由三类文件组成：

说明文件（唯一）
总目录文件（可多个）
子目录文件（可多个）

【1. 说明文件】

固定唯一：

/docs/AI_SYSTEM_RULES.md

作用：

定义所有规则
定义所有约束
定义目录生成逻辑
AI必须优先读取

【2. 总目录文件（Root Maps）】

命名规则：

/docs/root/ROOT_MAP_*.md

允许多个：

✔ /docs/root/ROOT_MAP_1.md
✔ /docs/root/ROOT_MAP_2.md
✔ /docs/root/ROOT_MAP_admin.md

规则：

所有总目录文件必须统一放入 /docs/root/ 文件夹
/docs/root/ 与 /docs/tree/ 同级，分别管理总目录和子目录
/docs 根目录只保留 AI_SYSTEM_RULES.md、TODO.md 等少量入口说明文件
禁止把 ROOT_MAP_*.md 直接堆放在 /docs 根目录，避免后续 ROOT_MAP_3、ROOT_MAP_4 等文件过多影响拖拽说明文件
每个总目录必须 ≤ 300 行
超过必须拆分成多个 ROOT_MAP
每个 ROOT_MAP 覆盖一部分目录结构
不允许重复覆盖同一目录（避免冲突）
⭐新增强制规则（关键修复）

ROOT_MAP 内禁止只写纯路径，每一条结构必须包含：

文件/目录路径
一句话自然语言说明（必须）

格式示例：

/src/user.ts —— 用户模块主入口，负责用户基础能力聚合
/src/components/NavBar.tsx —— 导航栏组件，影响全局顶部结构（修改UI入口）
/utils/auth.ts —— 登录鉴权工具函数集合

核心要求：
说明必须“短、具体、可定位”
必须让 AI 一眼知道“这个文件干什么”
必须包含“修改时影响范围提示”（如 UI / API / 数据层）
禁止空描述、禁止废话解释

【3. 子目录文件（Tree Maps）】

命名规则：

/docs/tree/<path>.md

例如：

/docs/tree/src.md
/docs/tree/components.md

规则：

每个子目录 ≤ 300 行
必须严格对应真实目录
不允许跨目录混写
⭐新增强制规则（关键修复）

TREE_MAP 同样禁止纯路径列表，每一项必须包含：

文件路径
文件作用说明
在该目录中的职责定位
（可选）修改提示

格式示例：

NavBar.tsx —— 顶部导航栏UI组件，控制主站导航结构（修改影响全局布局）
Sidebar.tsx —— 侧边栏导航组件，负责二级菜单展示与路由入口
hooks/useAuth.ts —— 登录状态Hook，提供全局鉴权状态

核心要求：
必须“按功能解释文件”
必须让 AI 能定位“该改哪个文件”
禁止只列目录结构
禁止出现无意义路径堆叠
==================================================
三、模块化开发拆分规则（强制）

开发任何项目时，必须按“按钮、功能、职责”进行最小模块拆分。

核心规则：

每一个按钮必须尽量拆分为独立组件文件
每一个独立功能必须拆分为独立模块文件
每一类相关模块可以使用文件夹分类管理
不得把多个无关按钮、多个无关功能堆在同一个大文件中
不得为了方便一次性修改而合并职责不同的模块

拆分目标：

修改某一个按钮时，只影响该按钮文件
修改某一个功能时，只影响该功能模块
修改某一个目录时，不应连锁破坏其他目录功能
AI 后续维护时，必须能通过目录说明快速找到目标文件

目录说明要求：

ROOT_MAP 和 TREE_MAP 必须同时体现“文件夹”和“文件”
每一个文件夹必须有一句话说明，说明该文件夹负责哪一类功能
每一个文件必须有一句话说明，说明该文件负责哪一个按钮、功能或职责
说明必须标明修改影响范围，如按钮UI、交互逻辑、API调用、状态管理、数据处理
禁止只列文件路径，禁止只列文件夹名，禁止没有说明的空目录结构

格式示例：

/src/components/buttons/ —— 按钮组件目录，集中管理可复用按钮UI（修改影响按钮视觉与交互）
/src/components/buttons/SubmitButton.tsx —— 提交按钮组件，只负责提交动作入口与禁用状态
/src/features/user/profile/ —— 用户资料功能目录，管理资料查看与编辑流程
/src/features/user/profile/ProfileEditor.tsx —— 用户资料编辑模块，只负责资料表单编辑UI
/src/features/user/profile/saveProfile.ts —— 保存用户资料功能，只负责提交资料 API 调用

AI修改规则：

新增按钮时，优先新增独立按钮文件，并在目录系统中补充说明
新增功能时，优先新增独立功能目录或模块文件，并在目录系统中补充说明
修改现有功能前，必须先通过 ROOT_MAP / TREE_MAP 定位最小职责文件
如果发现目标文件职责过多，必须先拆分再修改
拆分后必须保持原有调用关系清晰，不允许隐藏跨模块副作用
==================================================
四、300行限制规则（全局约束）

适用于所有 AI 可编辑代码文件：

✔ 所有代码文件 ≤ 300 行

如果超过：

必须执行拆分：

拆分规则：

按“职责拆分”（优先）
按“文件逻辑区域拆分”
保持调用关系不变
不允许功能重复

示例：

user.ts
→ user.create.ts
→ user.update.ts
→ user.query.ts

例外文件：

build/
dist/
.next/
自动生成文件

这些文件：

✔ 不限制行数
✘ AI禁止修改

==================================================
五、目录拆分规则（多ROOT机制）

当出现以下情况：

ROOT_MAP 超过 300 行
项目目录过大
结构复杂

必须拆分为多个 ROOT_MAP：

规则：

/docs/root/ROOT_MAP_1.md → 管理 src / components
/docs/root/ROOT_MAP_2.md → 管理 services / utils
/docs/root/ROOT_MAP_3.md → 管理 config / others

AI使用规则：

Step 1：
先读取 AI_SYSTEM_RULES.md

Step 2：
选择对应 ROOT_MAP

Step 3：
进入对应 TREE_MAP

Step 4：
定位真实代码

==================================================
六、目录生成规则（核心）

目录必须：

✔ 来源于真实文件系统
✔ 不允许预设不存在结构
✔ 必须随代码变化同步更新

当发生：

新增文件
删除文件
移动文件
重构目录

必须同步更新：

对应 ROOT_MAP
对应 TREE_MAP
==================================================
七、子目录生成规则（TREE MAP）

生成条件：

某目录文件数 > 5
或结构复杂
或频繁修改

必须生成：

/docs/tree/<directory>.md

内容仅包含：

✔ 当前目录文件列表
✔ 子目录结构
✔ 每个文件夹的一句话功能说明
✔ 每个文件的一句话职责说明
✔ 必要的修改影响范围提示
✘ 不允许无说明的纯路径堆叠

==================================================
八、AI执行流程（必须严格遵守）

Step 1：
读取 AI_SYSTEM_RULES.md

Step 2：
读取 /docs/root/ROOT_MAP_*

Step 3：
定位目标 TREE_MAP

Step 4：
读取最少必要代码文件（1~3个）

Step 5：
执行修改

Step 6：
同步更新：

ROOT_MAP
TREE_MAP
==================================================
九、禁止行为（强约束）

AI禁止：

✘ 全项目扫描
✘ 同时打开所有目录文件
✘ 忽略ROOT_MAP
✘ 跳过TREE_MAP直接改代码
✘ 不更新目录系统
✘ 破坏300行限制

==================================================
十、系统扩展规则（重要）
ROOT_MAP 可以无限拆分
TREE_MAP 可以无限拆分
说明文件永远只有一个
结构必须始终反映真实代码

==================================================
十一、核心总结

AI不是编辑器，是文件系统导航执行器
ROOT_MAP = 一级磁盘索引
TREE_MAP = 二级文件索引
CODE = 实际执行层
